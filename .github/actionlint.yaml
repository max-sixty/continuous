# The currently released dogfood workflow still contains `concurrency.queue`,
# which actionlint's schema rejects. Remove this repo-local suppression after
# the next release regenerates that workflow from the new template.
paths:
  '**/.github/workflows/tend-*.yaml':
    ignore:
      - unexpected key "queue" for "concurrency" section
